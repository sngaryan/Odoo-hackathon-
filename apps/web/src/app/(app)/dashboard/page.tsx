"use client";

import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import { apiRequest } from "@/lib/api";

type Overview = { kpis: { activePolicies: number; acknowledgedPolicies: number; openIssues: number; completedAudits: number }; riskList: { id: string; title: string; severity: string; status: string }[] };
const demoOverview: Overview = { kpis: { activePolicies: 18, acknowledgedPolicies: 146, openIssues: 7, completedAudits: 24 }, riskList: [{ id: "demo-1", title: "Supplier evidence renewal due in 8 days", severity: "HIGH", status: "IN_PROGRESS" }, { id: "demo-2", title: "Travel-emissions variance requires review", severity: "MEDIUM", status: "OPEN" }] };
const trend = [62, 68, 71, 76, 74, 82, 86, 89, 91, 88, 94, 96];

export default function DashboardPage() {
  const router = useRouter();
  const [overview, setOverview] = useState(demoOverview);
  useEffect(() => { const controller = new AbortController(); apiRequest<{ data: Overview }>("/dashboard/overview", { signal: controller.signal }).then((response) => response.json()).then((body) => { if (body.data) setOverview(body.data); }).catch(() => undefined); return () => controller.abort(); }, []);
  const kpis = [["Active policies", overview.kpis.activePolicies, "/governance/policies", "Open the policy register and employee acceptance status."], ["Open compliance issues", overview.kpis.openIssues, "/governance/issues", "Review audit-linked issues, severity, owner, due date, and status."], ["Completed audits", overview.kpis.completedAudits, "/governance/audits", "View assurance outcomes and audit scope."], ["Policy acknowledgements", overview.kpis.acknowledgedPolicies, "/governance/policies", "Review employee policy acceptance." ]] as const;
  return <section className="max-w-7xl"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="text-sm font-medium text-emerald-700">Executive governance</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">Governance command centre</h1><p className="mt-3 text-slate-600">Policies, assurance, compliance risk, and reporting readiness in one view.</p></div><span className="inline-flex w-fit items-center rounded-full bg-emerald-100 px-3 py-1 text-xs font-semibold text-emerald-800">Live Data Connected</span></div><div className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{kpis.map(([label, value, href, help]) => <button key={label} type="button" onClick={() => router.push(href)} title={help} className="group relative rounded-xl border border-slate-200 bg-white p-5 text-left shadow-sm transition duration-200 hover:-translate-y-1 hover:border-emerald-300 hover:shadow-md focus:outline-none focus:ring-2 focus:ring-emerald-500"><span className="pointer-events-none absolute -top-11 left-1/2 z-10 w-56 -translate-x-1/2 rounded-md bg-slate-950 px-3 py-2 text-center text-xs font-normal text-white opacity-0 shadow-lg transition group-hover:opacity-100 group-focus:opacity-100">{help}</span><p className="text-sm text-slate-500">{label}</p><p className="mt-3 text-3xl font-semibold tracking-tight tabular-nums">{value}</p><p className="mt-3 text-xs font-medium text-emerald-700">Open details →</p></button>)}</div><div className="mt-6 grid gap-6 lg:grid-cols-5"><article className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm lg:col-span-3"><div className="flex items-start justify-between"><div><h2 className="font-semibold">Control health trend</h2><p className="mt-1 text-sm text-slate-500">Monthly assurance score · trailing 12 months</p></div><span className="text-sm font-semibold text-emerald-700">96%</span></div><div className="mt-8 flex h-36 items-end gap-1.5">{trend.map((value, index) => <button type="button" key={`${value}-${index}`} title={`Assurance score: ${value}%`} onClick={() => router.push("/governance/audits")} className="group relative flex h-full flex-1 items-end rounded-t focus:outline-none focus:ring-2 focus:ring-emerald-500"><span className="pointer-events-none absolute -top-8 left-1/2 z-10 -translate-x-1/2 rounded bg-slate-950 px-2 py-1 text-xs text-white opacity-0 transition group-hover:opacity-100 group-focus:opacity-100">{value}%</span><span style={{ height: `${value}%` }} className="w-full rounded-t bg-emerald-500 transition duration-200 group-hover:bg-emerald-700" /></button>)}</div><div className="mt-3 flex justify-between text-xs text-slate-400"><span>Aug ’25</span><span>Jul ’26</span></div></article><article className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm lg:col-span-2"><h2 className="font-semibold">Reporting readiness</h2><p className="mt-1 text-sm text-slate-500">Download ready-to-share CSV evidence packs.</p><div className="mt-6 space-y-3">{[["Governance overview", "Policy and acknowledgement data"], ["Compliance register", "Issue ownership and lifecycle"], ["Audit register", "Completed and upcoming audits"]].map(([label, detail]) => <button type="button" key={label} onClick={() => router.push("/reports")} className="group flex w-full items-center justify-between border-b border-slate-100 pb-3 text-left last:border-0"><span><span className="block text-sm font-medium">{label}</span><span className="mt-1 block text-xs text-slate-500 group-hover:text-emerald-700">{detail}</span></span><span className="rounded-full bg-emerald-100 px-2.5 py-1 text-xs font-semibold text-emerald-800">Ready</span></button>)}</div></article></div><section className="mt-6"><div className="mb-3 flex items-center justify-between"><h2 className="font-semibold">Priority risk queue</h2><span className="text-sm text-slate-500">{overview.riskList.length} items requiring attention</span></div><div className="grid gap-3 md:grid-cols-2">{overview.riskList.map((risk) => <button type="button" key={risk.id} onClick={() => router.push("/governance/issues")} className="rounded-xl border border-amber-200 bg-amber-50 p-5 text-left transition duration-200 hover:-translate-y-0.5 hover:shadow-sm focus:outline-none focus:ring-2 focus:ring-amber-500"><p className="font-semibold text-amber-950">{risk.title}</p><p className="mt-2 text-sm text-amber-900">{risk.severity} severity · {risk.status.replaceAll("_", " ")}</p><p className="mt-4 text-xs font-medium text-amber-800">View issue details →</p></button>)}</div></section></section>;
}
